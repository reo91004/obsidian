---
type: concept
status: draft
context:
created: 2026-01-12
updated: 2026-03-15T19:39
aliases:
  - Toffoli Gate
  - CCNOT
  - 토폴리 게이트
source:
prefer-view: edit
tags:
---
# Toffoli Gate

## 한 줄 정의

두 개의 제어 비트가 모두 1일 때만 대상 비트를 반전시키는 3-큐비트 가역 논리 게이트 (CCNOT).

## 왜 중요한가

> **==양자 역학의 물리적 법칙은 가역적이어야 하기 때문에 중요하다.==**

- 양자 회로는 유니터리 연산만 허용하므로 가역 게이트가 필수적이다.
- 토폴리 게이트 하나로 모든 고전 논리 연산을 가역적으로 구현할 수 있다 (범용 가역 게이트).
- 양자 오류 정정, 양자 산술 회로 등에서 기본 빌딩 블록으로 쓰인다.

## 핵심 직관

토폴리 게이트는 3개의 입력 $(A,B,C)$과 3개의 출력 $(A',B',C')$을 가진다. 동작 원리는 매우 직관적이다.

- **제어 비트 (A, B):** 첫 번째와 두 번째 비트는 제어 역할을 한다. 이 값들은 출력에서 변하지 않고 그대로 유지된다 $(A'=A,\ B'=B)$.
- **대상 비트 (C):** 세 번째 비트는 대상(Target)이다. **앞의 두 제어 비트가 모두 1일 때만** 세 번째 비트의 값을 뒤집는다(NOT 연산). 그 외의 경우에는 원래 값을 유지한다.

$$C' = C \oplus (A \cdot B)$$

## 형식적 정의 / 수식

토폴리 게이트의 진리표:

| **입력 (A, B, C)** | **출력 (A', B', C')** | **비고**            |
| ---------------- | ------------------- | ----------------- |
| 0, 0, 0          | 0, 0, 0             | 변화 없음             |
| 0, 1, 0          | 0, 1, 0             | 변화 없음             |
| 1, 0, 0          | 1, 0, 0             | 변화 없음             |
| **1, 1, 0**      | **1, 1, 1**         | **C 반전 (0 -> 1)** |
| 1, 1, 1          | 1, 1, 0             | **C 반전 (1 -> 0)** |

$8 \times 8$ 유니터리 행렬로 표현하면, 계산 기저 $|110\rangle \leftrightarrow |111\rangle$만 교환하고 나머지는 항등이다.

## 예시

### 가역성 (Reversibility)

일반적인 NAND나 AND 게이트는 결과값만 보고 입력값이 무엇이었는지 완벽하게 알아낼 수 없다(정보의 손실 발생). 하지만 토폴리 게이트는 출력을 다시 입력으로 넣으면 원래의 값이 나온다. 즉, **정보 손실이 없는 가역 계산**이 가능하다.

### 범용성 (Universality)

앞서 언급한 NAND 게이트처럼, 토폴리 게이트 하나만 있으면 모든 종류의 논리 연산을 구현할 수 있다.

- C의 초기값을 1로 고정하면, 출력 $C'$은 $1 \oplus (A \cdot B)$가 되어 **NAND 게이트**처럼 동작한다.
- A를 1로 고정하고 B를 제어 비트로 쓰면 **CNOT(XOR) 게이트**가 된다.

## 자주 헷갈리는 점

- **토폴리 게이트는 고전 가역 게이트이다**: 양자 게이트로도 사용되지만, 원래 고전 가역 컴퓨팅을 위해 제안되었다.
- **양자 범용성과 고전 범용성은 다르다**: 토폴리 게이트만으로는 양자 범용(universal quantum) 게이트 세트를 구성할 수 없다. Hadamard 같은 단일 큐비트 게이트가 추가로 필요하다.

## 상위 개념
- [[Quantum Computing]]

## 하위 개념

## 관련 개념
- [[Qubit]]

## 같이 볼 것